CREATE TABLE [dbo].[orchard_Orchard_Blogs_BlogPartRecord] (
    [Id]          INT            NOT NULL,
    [Description] NVARCHAR (MAX) NULL,
    [PostCount]   INT            NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

