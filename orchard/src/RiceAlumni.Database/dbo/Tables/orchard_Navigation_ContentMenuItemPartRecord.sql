CREATE TABLE [dbo].[orchard_Navigation_ContentMenuItemPartRecord] (
    [Id]                       INT NOT NULL,
    [ContentMenuItemRecord_id] INT NULL,
    PRIMARY KEY CLUSTERED ([Id] ASC)
);

