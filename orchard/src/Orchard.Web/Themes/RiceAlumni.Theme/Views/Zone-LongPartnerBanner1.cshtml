<div class="sixteen columns headitadvert">
    <ul id="slider1">
        @foreach (var item in Model.Items)
        {
            <li>
                @Display(item)
                <div class="clear"></div>
            </li>
        }
    </ul>
</div>
